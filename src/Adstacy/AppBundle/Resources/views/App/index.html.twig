{% extends 'AdstacyAppBundle::layout.html.twig' %}

{% block content %}
  <div id="ads" class="jscroll clearfix">
    {% include 'AdstacyAppBundle:Includes:ads.html.twig' with {ads: ads} %}
  </div>
{% endblock %}

{% block javascripts %}
  {{ parent() }}
  <script type="text/javascript">
    $(function() {
      $('.jscroll').jscroll({
        nextSelector: 'a.next-page',
        callback: function(a, b) {
          var $ads = $('.jscroll-added:last .ad');
          $masonry = $('#ads').masonry('appended', $ads);
          $ads.find('img.lazy').lazyload();
        }
      });
    });
  </script>
{% endblock %}
