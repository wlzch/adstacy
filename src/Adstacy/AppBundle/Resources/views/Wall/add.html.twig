{% extends 'AdstacyAppBundle::layout.html.twig' %}

{% block content %}
  {{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_rest(form) }}

    <input type="submit">
  {{ form_end(form) }}
{% endblock %}
