{% extends 'AdstacyAppBundle::layout.html.twig' %}

{% block content %}
<div class="settings-container panel panel-default">
  <section class="panel-section">
    <h1 class="title">User Info</h1>
    {{ form_start(form, { attr: { class: 'form-horizontal', role: 'form' } }) }}
      {% set user = form.vars.data %}

      <div class="form-group">
        {{ form_label(form.image, label|default(null), { label_attr: { class: 'control-label' } }) }}
        <div class="form-widget">
          {{ form_widget(form.image, { attr: { class: 'form-control' } }) }}
        </div>
      </div>


      <div class="form-group">
        {{ form_label(form.username, label|default(null), { label_attr: { class: 'control-label' } }) }}
        <div class="form-widget">
          {{ form_widget(form.username, { attr: { class: 'form-control' } }) }}
        </div>
      </div>

      <div class="form-group">
        <label class="control-label">Password</label>
        <div class="form-widget">
          <a href="{{ path('fos_user_change_password') }}">{{"form_profile.change_password"|trans}}</a>
        </div>
      </div>

      <div class="form-group">
        {{ form_label(form.email, label|default(null), { label_attr: { class: 'control-label' } }) }}
        <div class="form-widget">
          {{ form_widget(form.email, { attr: { class: 'form-control' } }) }}
        </div>
      </div>

      <div class="form-group">
        {{ form_label(form.realName, label|default(null), { label_attr: { class: 'control-label' } }) }}
        <div class="form-widget">
          {{ form_widget(form.realName, { attr: { class: 'form-control' } }) }}
        </div>
      </div>

      <div class="form-group">
        {{ form_label(form.about, label|default(null), { label_attr: { class: 'control-label' } }) }}
        <div class="form-widget">
          {{ form_widget(form.about, { attr: { class: 'form-control' } }) }}
        </div>
      </div>

      <div class="form-group">
        <label class="control-label">&nbsp;</label>
        <div class="form-widget">
          {{ form_widget(form.save, { attr: { class: 'form-control btn btn-primary' } }) }}
        </div>
      </div>

      <div class="form-group">
        <label class="control-label">&nbsp;</label>
        <div class="form-widget">
          <a href="{{ path('fos_user_resetting_request') }}">{{"form_profile.forgot_password"|trans}}</a>
        </div>
      </div>

      {{ form_rest(form) }}

    {{ form_end(form) }}
  </section>

  <section class="panel-section">
    <h1 class="title">Facebook Connect</h1>
    <div class="form-horizontal">
      <div class="form-group">
        <label class="control-label">Status</label>
        <div class="form-widget">
          {% if user.facebookId %}
            <p>{{"form_profile.connected"|trans({'%name%': user.facebookRealName})}}</p>
          {% else %}
            <p>{{"form_profile.not_connected"|trans}}</p>
          {% endif %}
        </div>
      </div>
      <div class="form-group">
        <label class="control-label">&nbsp;</label>
        <div class="form-widget">
          {% if user.facebookId %}
            <a href="{{ path('adstacy_app_user_disconnect', {service: 'facebook'}) }}" class="btn btn-facebook">{{"form_profile.disconnect"|trans({'%service%': 'Facebook'}) }}</a>
          {% else %}
            <a href="{{ path('hwi_oauth_service_redirect', {service: 'facebook'}) }}" class="btn btn-facebook">{{"form_profile.connect"|trans({'%service%': 'Facebook'}) }}</a>
          {% endif %}
        </div>
      </div>
    </div>
  </section>

  <section class="panel-section">
    <h1 class="title">Twitter Connect</h1>
    <div class="form-horizontal">
      <div class="form-group">
        <label class="control-label">Status</label>
        <div class="form-widget">
          {% if user.twitterId %}
            <p>{{"form_profile.connected"|trans({'%name%': user.twitterRealName}) }}</p>
          {% else %}
            <p>{{"form_profile.not_connected"|trans}}</p>
          {% endif %}
        </div>
      </div>
      <div class="form-group">
        <label class="control-label">&nbsp;</label>
        <div class="form-widget">
          {% if user.twitterId %}
            <a href="{{ path('adstacy_app_user_disconnect', {service: 'twitter'}) }}" class="btn btn-twitter">{{"form_profile.disconnect"|trans({'%service%': 'Twitter'}) }}</a>
          {% else %}
            <a href="{{ path('hwi_oauth_service_redirect', {service: 'twitter'}) }}" class="btn btn-twitter">{{"form_profile.connect"|trans({'%service%': 'Twitter'}) }}</a>
          {% endif %}
        </div>
      </div>
    </div>
  </section>
</div>
{% endblock %}
